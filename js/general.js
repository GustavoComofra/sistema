function mostrarOcultarBarra() {
    var divLaretal = document.getElementById('divLateral');
    //divLaretal.style.display = 'block';
    // Verifica si el div está actualmente visible
    if (divLaretal.style.display === 'none') {
      // Si está oculto, lo muestra
      divLaretal.style.display = 'block';
    } else {
      // Si está visible, lo oculta
      divLaretal.style.display = 'none';
    }
  }

  function mostrarOcultarVenta() {
    var divVenta = document.getElementById('divBtnVenta');
  
    // Verifica si el div está actualmente visible
    if (divVenta.style.display === 'none') {
      // Si está oculto, lo muestra
      divVenta.style.display = 'block';
    } else {
      // Si está visible, lo oculta
      divVenta.style.display = 'none';
    }
  }

  function mostrarOcultarCalidad() {
    var divCalidad = document.getElementById('divBtnCalidad');
  
    // Verifica si el div está actualmente visible
    if (divCalidad.style.display === 'none') {
      // Si está oculto, lo muestra
      divCalidad.style.display = 'block';
    } else {
      // Si está visible, lo oculta
      divCalidad.style.display = 'none';
    }
  }

  function mostrarOcultarIng() {
    var divIng = document.getElementById('divBtnIng');
  
    // Verifica si el div está actualmente visible
    if (divIng.style.display === 'none') {
      // Si está oculto, lo muestra
      divIng.style.display = 'block';
    } else {
      // Si está visible, lo oculta
      divIng.style.display = 'none';
    }
  }

  function mostrarOcultarRRHH() {
    var divRRHH = document.getElementById('divBtnRRHH');
  
    // Verifica si el div está actualmente visible
    if (divRRHH.style.display === 'none') {
      // Si está oculto, lo muestra
      divRRHH.style.display = 'block';
    } else {
      // Si está visible, lo oculta
      divRRHH.style.display = 'none';
    }
  }

  function mostrarOcultarMant() {
    var divMant = document.getElementById('divBtnMant');
  
    // Verifica si el div está actualmente visible
    if (divMant.style.display === 'none') {
      // Si está oculto, lo muestra
      divMant.style.display = 'block';
    } else {
      // Si está visible, lo oculta
      divMant.style.display = 'none';
    }
  }

  function mostrarOcultarList() {
    var divList = document.getElementById('divBtnList');
  
    // Verifica si el div está actualmente visible
    if (divList.style.display === 'none') {
      // Si está oculto, lo muestra
      divList.style.display = 'block';
    } else {
      // Si está visible, lo oculta
      divList.style.display = 'none';
    }
  }